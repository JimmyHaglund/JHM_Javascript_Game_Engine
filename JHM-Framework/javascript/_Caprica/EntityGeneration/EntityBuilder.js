class EntityBuilder {
    EntityBuilder(layer, physicsSpace, camera, loop) {
        this._layer = layer;
        this._physics = physicsSpace;
        this._camera = camera;
        this._loop = loop;
    }
}
