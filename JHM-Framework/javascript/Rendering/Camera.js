class Camera {
    constructor(renderSpace, transform) {
        this.positionY = 0;
        this._transform = transform;
        this._renderSpace = renderSpace;
    }
    get centreX() { return; }
}
